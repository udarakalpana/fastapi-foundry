[project]
name = "fastapimvc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kalpana", email = "kalpanaudara058@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
fastapimvc = "fastapimvc:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
